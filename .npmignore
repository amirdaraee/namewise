# Source files (dist/ is what gets published)
src/

# Tests
tests/

# CI/CD
.github/

# Dev config
vitest.config.ts
tsconfig.json

# Legacy unused artifact
eng.traineddata
.ocr-cache/

# Dev files
*.log
.env*

# Dev-only files that .gitignore covers but .npmignore must repeat: having a
# .npmignore makes npm ignore .gitignore entirely, so these shipped in the
# published tarball up to 1.2.1.
.claude/
coverage/
smoke-corpus/
scripts/
eslint.config.js
CLAUDE.md
docs/superpowers/
